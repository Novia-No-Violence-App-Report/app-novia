<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/chatLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginLeft="40dp"
    android:paddingBottom="20dp">

    <TextView
        android:id="@+id/tv_outgoing_message"
        style="@style/Text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/chat_outgoing_bubble"
        android:paddingVertical="10dp"
        android:paddingLeft="16dp"
        android:paddingRight="40dp"
        android:text="@string/hello_saya_novia"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_outgoing_time"
        app:layout_constraintLeft_toLeftOf="@id/tv_outgoing_message"
        app:layout_constraintTop_toBottomOf="@id/tv_outgoing_message"
        android:layout_marginLeft="16dp"
        android:text="@string/_09_00"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

</androidx.constraintlayout.widget.ConstraintLayout>