<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/chatLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginEnd="40dp"
    android:paddingBottom="20dp">

    <TextView
        android:id="@+id/tv_incoming_message"
        style="@style/Text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/chat_incoming_bubble"
        android:paddingVertical="10dp"
        android:paddingLeft="40dp"
        android:paddingRight="16dp"
        android:text="@string/hello_saya_novia"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_incoming_time"
        app:layout_constraintRight_toRightOf="@id/tv_incoming_message"
        app:layout_constraintTop_toBottomOf="@id/tv_incoming_message"
        android:layout_marginEnd="16dp"
        android:text="@string/_09_00"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>

</androidx.constraintlayout.widget.ConstraintLayout>